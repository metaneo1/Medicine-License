@Html.DevExpress().CallbackPanel(settings => {
    settings.Name = "CallbackPanel1";
    settings.CallbackRouteValues = new { Controller = "Message", Action = "CallbackPanel1Partial", Filter = 2 };
    settings.Width = 500;
	settings.Height = 300;

    settings.SetContent(() => {
		ViewContext.Writer.Write("content");
	});
}).GetHtml()